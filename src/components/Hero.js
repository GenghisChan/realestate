import React from 'react';
import '../styles/Hero.css'

const styles = {
  hero: {
    backgroundImage: `url(${Image})`,
    opacity: .8
  }
};

const Hero = () => {
  return(
    <div className="container hero">

    </div>
  )
}

export default Hero;
